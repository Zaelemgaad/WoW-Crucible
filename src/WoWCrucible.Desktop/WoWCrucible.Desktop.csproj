<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <ApplicationIcon>Assets\Branding\WoWCrucible.ico</ApplicationIcon>
    <!-- A copied single-file build must carry Avalonia's native renderer libraries
         with it. Without this, Windows reports a misleading "Unknown Hard Error". -->
    <IncludeNativeLibrariesForSelfExtract>true</IncludeNativeLibrariesForSelfExtract>
    <IncludeNativeDependencySymbols Condition="'$(IncludeNativeDependencySymbols)' == ''">false</IncludeNativeDependencySymbols>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Avalonia" Version="12.1.0" />
    <PackageReference Include="Avalonia.Desktop" Version="12.1.0" />
    <PackageReference Include="Avalonia.Skia" Version="12.1.0" />
    <PackageReference Include="Avalonia.Themes.Fluent" Version="12.1.0" />
    <PackageReference Include="Avalonia.Fonts.Inter" Version="12.1.0" />
  </ItemGroup>

  <ItemGroup>
    <AvaloniaResource Include="Assets\Branding\WoWCrucible.ico" />
    <AvaloniaResource Include="Assets\Branding\WoWCrucible-128.png" />
    <ProjectReference Include="..\WoWCrucible.Core\WoWCrucible.Core.csproj" />
    <None Include="..\..\third_party\StormLib\win-x64\StormLib.dll"
          Link="StormLib.dll"
          CopyToOutputDirectory="PreserveNewest"
          CopyToPublishDirectory="PreserveNewest" />
    <None Include="..\..\third_party\CascLib\win-x64\CascLib.dll"
          Link="CascLib.dll"
          CopyToOutputDirectory="PreserveNewest"
          CopyToPublishDirectory="PreserveNewest" />
    <None Include="..\..\docs\CLI-REFERENCE.md"
          Link="docs\CLI-REFERENCE.md"
          CopyToOutputDirectory="PreserveNewest"
          CopyToPublishDirectory="PreserveNewest" />
    <None Include="..\..\docs\DEVBUG-MODE.md"
          Link="docs\DEVBUG-MODE.md"
          CopyToOutputDirectory="PreserveNewest"
          CopyToPublishDirectory="PreserveNewest" />
  </ItemGroup>
  <!-- Native package PDBs are optional debugger inputs, not renderer dependencies.
       Filter before build/publish asset copying instead of deleting them afterwards. -->
  <Target Name="ExcludeNativeDependencySymbols" AfterTargets="ResolvePackageAssets"
          Condition="'$(IncludeNativeDependencySymbols)' != 'true'">
    <ItemGroup>
      <NativeCopyLocalItems Remove="@(NativeCopyLocalItems)"
          Condition="'%(NativeCopyLocalItems.Extension)' == '.pdb' and ('%(NativeCopyLocalItems.Filename)' == 'libSkiaSharp' or '%(NativeCopyLocalItems.Filename)' == 'libHarfBuzzSharp')" />
      <RuntimeTargetsCopyLocalItems Remove="@(RuntimeTargetsCopyLocalItems)"
          Condition="'%(RuntimeTargetsCopyLocalItems.Extension)' == '.pdb' and ('%(RuntimeTargetsCopyLocalItems.Filename)' == 'libSkiaSharp' or '%(RuntimeTargetsCopyLocalItems.Filename)' == 'libHarfBuzzSharp')" />
    </ItemGroup>
  </Target>
  <PropertyGroup>
    <CrucibleDesktopShortcutSettings Condition="'$(CrucibleDesktopShortcutSettings)' == ''">$(MSBuildProjectDirectory)/../../.local/desktop-shortcut.json</CrucibleDesktopShortcutSettings>
    <CrucibleDesktopShortcutUpdater>$(MSBuildProjectDirectory)/../../scripts/Update-DesktopShortcut.ps1</CrucibleDesktopShortcutUpdater>
  </PropertyGroup>
  <!-- Opt-in, local registration. Failed builds never advance the shortcut. -->
  <Target Name="UpdateBuiltDesktopShortcut" AfterTargets="Build"
          Condition="'$(OS)' == 'Windows_NT' and '$(DesignTimeBuild)' != 'true' and Exists('$(CrucibleDesktopShortcutSettings)') and Exists('$(TargetDir)$(AssemblyName).exe')">
    <Exec Command="&quot;$(SystemRoot)\System32\WindowsPowerShell\v1.0\powershell.exe&quot; -NoProfile -NonInteractive -ExecutionPolicy Bypass -File &quot;$(CrucibleDesktopShortcutUpdater)&quot; -ExecutablePath &quot;$(TargetDir)$(AssemblyName).exe&quot; -SettingsPath &quot;$(CrucibleDesktopShortcutSettings)&quot;" />
  </Target>
  <Target Name="UpdatePublishedDesktopShortcut" AfterTargets="Publish"
          Condition="'$(OS)' == 'Windows_NT' and '$(DesignTimeBuild)' != 'true' and Exists('$(CrucibleDesktopShortcutSettings)') and Exists('$(PublishDir)$(AssemblyName).exe')">
    <Exec Command="&quot;$(SystemRoot)\System32\WindowsPowerShell\v1.0\powershell.exe&quot; -NoProfile -NonInteractive -ExecutionPolicy Bypass -File &quot;$(CrucibleDesktopShortcutUpdater)&quot; -ExecutablePath &quot;$(PublishDir)$(AssemblyName).exe&quot; -SettingsPath &quot;$(CrucibleDesktopShortcutSettings)&quot;" />
  </Target>
</Project>
