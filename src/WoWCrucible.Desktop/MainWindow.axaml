<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="using:WoWCrucible.Desktop.Controls"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="1440" d:DesignHeight="900"
        x:Class="WoWCrucible.Desktop.MainWindow"
        Title="WoW Crucible — Desktop Preview"
        Width="1440" Height="900" MinWidth="1050" MinHeight="680"
        WindowStartupLocation="CenterScreen">
    <Grid RowDefinitions="56,*,30" ColumnDefinitions="224,*,300">
        <Border Grid.Row="0" Grid.ColumnSpan="3" BorderBrush="#2B3445" BorderThickness="0,0,0,1">
            <Grid ColumnDefinitions="224,*,Auto" Margin="14,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Spacing="9">
                    <Border Width="28" Height="28" CornerRadius="6" Background="#C58A2B">
                        <TextBlock Text="C" Foreground="#0B0E13" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <TextBlock Text="WOW CRUCIBLE" FontWeight="SemiBold" FontSize="15" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                    <Button Classes="accent" Content="Open DBC" Click="OpenDbcClick" />
                    <Button Content="Open M2" Click="OpenM2Click" />
                    <Button Content="Save" Click="SaveClick" />
                    <Button Content="Logs" Click="OpenLogsClick" />
                    <TextBox x:Name="SearchBox" Width="280" PlaceholderText="Search loaded table…" TextChanged="SearchChanged" />
                </StackPanel>
                <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                    <Border Padding="9,5" CornerRadius="5" Background="#13251F" BorderBrush="#285845" BorderThickness="1">
                        <TextBlock Text="AVALONIA PREVIEW" Foreground="#79D7A8" FontSize="11" FontWeight="SemiBold" />
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Grid.Column="0" BorderBrush="#2B3445" BorderThickness="0,0,1,0" Background="#0E1219">
            <Grid RowDefinitions="Auto,Auto,*,Auto" Margin="12">
                <TextBlock Text="WORKSPACE" Foreground="#7F8A9F" FontSize="10" FontWeight="Bold" Margin="6,4,0,9" />
                <StackPanel Grid.Row="1" Spacing="4">
                    <Button HorizontalContentAlignment="Left" Content="▦  DBC tables" />
                    <Button HorizontalContentAlignment="Left" Content="◇  MPQ patches" />
                    <Button HorizontalContentAlignment="Left" Content="⬡  Assets &amp; models" />
                    <Button HorizontalContentAlignment="Left" Content="⚙  Server &amp; SQL" />
                </StackPanel>
                <Border Grid.Row="3" Padding="10" CornerRadius="6" Background="#131925" BorderBrush="#273044" BorderThickness="1">
                    <StackPanel Spacing="5">
                        <TextBlock Text="Performance mode" FontWeight="SemiBold" />
                        <TextBlock Text="No animations · direct-rendered tables · background file I/O" TextWrapping="Wrap" Foreground="#8E99AD" FontSize="11" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Grid.Column="1" RowDefinitions="42,*" Background="#090C11">
            <Border BorderBrush="#2B3445" BorderThickness="0,0,0,1" Background="#0E131C">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom">
                    <Border x:Name="DocumentTab" IsVisible="False" Padding="15,11,15,10" BorderBrush="#C58A2B" BorderThickness="0,0,0,2">
                        <TextBlock x:Name="DocumentTabText" Text="Table" FontSize="12" />
                    </Border>
                </StackPanel>
            </Border>
            <Grid Grid.Row="1">
                <Grid x:Name="WelcomePanel" VerticalAlignment="Center" HorizontalAlignment="Center" Width="520">
                    <Border CornerRadius="12" Padding="34" BorderBrush="#2A3447" BorderThickness="1">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0%,0%" EndPoint="100%,100%">
                                <GradientStop Color="#182233" Offset="0" />
                                <GradientStop Color="#0E131C" Offset="1" />
                            </LinearGradientBrush>
                        </Border.Background>
                        <StackPanel Spacing="13">
                            <TextBlock Text="Crucible, rebuilt for speed." FontSize="28" FontWeight="SemiBold" />
                            <TextBlock Text="This is the first native Avalonia workspace. Open a WotLK WDBC table to test the custom virtual viewport against a real client or server dataset." Foreground="#A7B0C0" TextWrapping="Wrap" LineHeight="21" />
                            <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,8,0,0">
                                <Button Classes="accent" Content="Open a DBC" Click="OpenDbcClick" />
                                <Button Content="Preview an M2" Click="OpenM2Click" />
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>
                <local:VirtualDbcView x:Name="DbcView" IsVisible="False" />
                <local:M2PreviewView x:Name="M2View" IsVisible="False" />
            </Grid>
        </Grid>

        <Border Grid.Row="1" Grid.Column="2" BorderBrush="#2B3445" BorderThickness="1,0,0,0" Background="#0E1219">
            <Grid RowDefinitions="Auto,Auto,*" Margin="14">
                <TextBlock Text="INSPECTOR" Foreground="#7F8A9F" FontSize="10" FontWeight="Bold" Margin="2,4,0,12" />
                <StackPanel Grid.Row="1" Spacing="8">
                    <TextBlock x:Name="InspectorTitle" Text="Nothing selected" FontSize="17" FontWeight="SemiBold" />
                    <TextBlock x:Name="InspectorSummary" Text="Open a table or model to begin." TextWrapping="Wrap" Foreground="#939EB1" />
                </StackPanel>
                <Border Grid.Row="2" Margin="0,18,0,0" CornerRadius="7" BorderBrush="#283247" BorderThickness="1" Background="#0A0E15">
                    <TextBlock x:Name="InspectorDetail" Text="Table metadata and selection details appear here." Margin="14" TextWrapping="Wrap" Foreground="#7F8A9F" FontFamily="Cascadia Mono,Consolas" FontSize="11" />
                </Border>
            </Grid>
        </Border>

        <Border Grid.Row="2" Grid.ColumnSpan="3" BorderBrush="#2B3445" BorderThickness="0,1,0,0" Background="#0B0F16">
            <Grid ColumnDefinitions="*,Auto" Margin="12,0">
                <TextBlock x:Name="StatusText" Text="Ready" VerticalAlignment="Center" Foreground="#98A2B4" FontSize="11" />
                <TextBlock x:Name="RenderText" Grid.Column="1" Text="Viewport idle" VerticalAlignment="Center" Foreground="#758197" FontSize="11" />
            </Grid>
        </Border>
    </Grid>
</Window>
